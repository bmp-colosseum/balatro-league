{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "//": "Use the portable Dockerfile (build context = repo root so workspaces link). Set the service Root Directory to `/`.",
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/tour/Dockerfile"
  },
  "deploy": {
    "//": "Build + start (prisma db push → next start on $PORT) live in the Dockerfile.",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
